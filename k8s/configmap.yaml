apiVersion: v1
kind: ConfigMap
metadata:
  name: oficina-config
  namespace: oficina
data:
  SPRING_PROFILES_ACTIVE: "prod"
  SERVER_PORT: "8080"
  MANAGEMENT_HEALTH_MAIL_ENABLED: "false"
  NEW_RELIC_APP_NAME: "oficina-app"
  NEW_RELIC_LOG_LEVEL: "info"
